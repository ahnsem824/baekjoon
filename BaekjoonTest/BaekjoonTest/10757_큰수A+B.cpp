#include <iostream>
#include<vector>
#include<string>
using namespace std;

void Å«¼öAB_10757()
{
	ios::sync_with_stdio(false), cout.tie(0), cin.tie(0);
	string a;
	string b; 

	string max;
	string min;

	cin >> a >> b;
	if (a.length() >= b.length())
	{
		max = a;
		min = b;
	}
	else
	{
		min = a;
		max = b;
	}
	
	
	for (int i = 0; i < min.length(); i++)
	{
	
	}


}