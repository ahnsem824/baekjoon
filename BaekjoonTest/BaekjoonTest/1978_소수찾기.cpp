#include <iostream>
using namespace std;

void 소수찾기_1978()
{
	ios::sync_with_stdio(false), cout.tie(0), cin.tie(0); // 동기화를 끊는다

	int n;

	cin >> n; 
	for (int i = 0; i < n; i++)
	{
		int a;

		cin >> a;

		for (int j = 0; j < a; j++)
		{
			if (j < 2)
			{
				continue;
			}
			else
			{

			}
		}
	}



}